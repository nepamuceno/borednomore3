[settings]
# Time between wallpaper changes (seconds)
interval = 300

# Wallpaper directory (absolute or relative to config file)
directory = ~/Pictures/Wallpapers

# Number of transition frames (5-100, higher = smoother)
frames = 10

# Delay between frames (seconds, 0.0001-1.0)
speed = 0.001

# Transition IDs (comma-separated or ranges, e.g., 1,5,10-20)
# Leave empty for all transitions
transitions = 

# Randomize transition order (True/False)
randomize = False

# Randomize wallpaper order (True/False)
randomize_wallpapers = False

# Keep previous image during transition (True/False)
keep_image = False
```

---

## 4. `/home/deb/borednomore3/borednomore3/conf/borednomore3.list-example`
```
# Wallpaper patterns (one per line)
# Glob patterns supported

*.jpg
*.jpeg
*.png
*.webp

# Add more patterns below
