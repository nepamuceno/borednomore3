# BNM3 Backends

The engine room of BoredNoMore3.

- **borednomore3.py**: The classic media management logic.
- **bnm3d.py**: The modern daemon-style backend. Supports the `-r` flag for randomization.

## Configuration
Backends read from `/etc/borednomore3/` by default.
